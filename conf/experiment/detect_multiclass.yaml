# @package _global_

run:
  exp_id: D3-multiclass
  task: eval_sam

method:
  idr_source: afp
  sam_mode: none
  query_source: online
  prompt:
    mode: multiclass
    classes: [car, truck, person, bicycle, motorcycle, bus, train]
    use_bbox: true
    strategy: all_classes_topk_merge

tags:
  - detection
  - multiclass
