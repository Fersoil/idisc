# @package _global_

run:
  exp_id: E7-concat-multiclass
  task: eval

method:
  idr_source: afp
  sam_mode: concat
  query_source: online
  prompt:
    mode: multiclass
    classes: [car, truck, person, bicycle, motorcycle, bus, train]
    use_bbox: true
    strategy: all_classes_topk_merge

tags:
  - concat
  - multiclass
