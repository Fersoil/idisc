# @package _global_

run:
  exp_id: E4-branch-singleclass
  task: eval

method:
  idr_source: raw
  sam_mode: none
  query_source: online
  prompt:
    mode: singleclass
    classes: [car, truck, person, bicycle, motorcycle, bus, train]
    use_bbox: true
    strategy: top1_by_confidence

tags:
  - branch
  - singleclass
