n_iters: 5000
lr: 5.0e-5
batch_size: 1
val_interval: 500
output_dir: output/models/${run.exp_id}
amp_dtype: bfloat16

temporal:
  enabled: false
  weight: 0.0
  confidence_threshold: 0.3
  sam_checkpoint: ${paths.sam_checkpoint}
  prompt:
    classes: [car]
