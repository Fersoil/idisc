# @package _global_

run:
  exp_id: E8-concat-singleclass
  task: eval

method:
  idr_source: afp
  sam_mode: concat
  query_source: online
  prompt:
    mode: singleclass
    classes: [car, truck, person, bicycle, motorcycle, bus, train]
    use_bbox: true
    strategy: top1_by_confidence

tags:
  - concat
  - singleclass
