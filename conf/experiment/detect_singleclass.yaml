# @package _global_

run:
  exp_id: D2-singleclass
  task: eval_sam

method:
  idr_source: afp
  sam_mode: none
  query_source: online
  prompt:
    mode: singleclass
    classes: [car, truck, person, bicycle, motorcycle, bus, train]
    use_bbox: true
    strategy: top1_by_confidence

tags:
  - detection
  - singleclass
